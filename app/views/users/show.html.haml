= provide(:title, @user.firstname)
.row
  %aside.span4
    %section
      %h1
        = gravatar_for @user
        = @user.firstname
        = @user.email
= link_to 'Edit', edit_user_path(@user)
|
= link_to 'Back', users_path
