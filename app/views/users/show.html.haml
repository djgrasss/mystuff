= provide(:title, @user.name)
.row
  %aside.span4
    %section
      %h1
        = gravatar_for @user
        = @user.name
        = @user.email
= link_to 'Edit', edit_user_path(@user)
|
= link_to 'Back', users_path
